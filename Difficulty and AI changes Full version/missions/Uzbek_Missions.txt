SHY_missions_1 = { #Uzbek
	slot = 1
	generic = no
	ai = yes
	potential = {
		tag = SHY
		NOT = { map_setup = map_setup_random }
	}
	has_country_shield = yes
	weakening_of_timurid_dynasty = {
		icon = mission_unite_home_region
		required_missions = {  }
		#provinces_to_highlight = {
		#	province_id = 1816
		#}
		#position = 1
		trigger = {
			custom_trigger_tooltip = {
				tooltip = weakening_of_timurid_dynasty_tooltip
				has_global_flag = death_of_shah_rukh
			}
		}
		effect = {
			add_country_modifier = {
				name = "uzbek_grand_host"
				duration = 5475
			}
			add_yearly_manpower = 2.5
			add_casus_belli = {
				target = TIM
				type = cb_horde_invasion
				months = 180
			}
			add_casus_belli = {
				target = TRS
				type = cb_horde_invasion
				months = 180
			}
			transoxiana_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			khiva_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			arys_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
		}
	}
	claim_rulership_over_samarkand = {
		icon = mission_central_asian_city
		required_missions = { weakening_of_timurid_dynasty }
		provinces_to_highlight = {
			area = transoxiana_area
			NOT = {
				owned_by = ROOT
				is_core = ROOT
			}
		}
		#position = 1
		trigger = {
			transoxiana_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			ferghana_area = {
				limit = {
					NOT = { province_id = 460 }
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			termez_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			transcaspia_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
		}
	}
	uzb_invade_herat = {
		icon = mission_arabian_fort
		required_missions = { claim_rulership_over_samarkand }
		provinces_to_highlight = {
			area = transoxiana_area
			area = khiva_area
			NOT = {
				owned_by = ROOT
				is_core = ROOT
			}
		}
		#position = 1
		trigger = {
			tag = BUK
			transoxiana_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
			khiva_area = {
				type = all
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		effect = {
			merv_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			balkh_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
			herat_area = {
				limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
				add_permanent_claim = ROOT
			}
		}
	}
}