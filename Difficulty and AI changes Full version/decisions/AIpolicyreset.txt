country_decisions = { 
    AIpolicy_reset = {
        major = yes
        potential = {
			adm_tech = 18
			NOT = { adm_tech = 19 }
			ai = yes
			NOT = { has_country_flag = policy_reset_flag }
        }
        allow = {
			is_at_war = no
        }
        effect = {
			add_country_modifier = {
				name = policy_reset
				duration = 30
			}
			set_country_flag = policy_reset_flag
        }
        ai_will_do = {
            factor = 1
        }
    }
}