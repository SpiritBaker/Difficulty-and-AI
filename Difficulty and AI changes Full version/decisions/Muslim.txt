country_decisions = {
	
	unify_islam = {
		major = yes
		potential = {
			has_dlc = "Star and Crescent"
			#ai = no
			religion_group = muslim
			num_of_cities = 10
			NOT = {
				has_country_flag = unified_islam
			}
			NOT = {
				has_global_flag = unified_islam_global
			}
			NOT = { tag = PAP } # NO
			is_emperor_of_china = no
			NOT = { has_reform = shogunate }
			NOT = { has_reform = celestial_empire }
		}
		provinces_to_highlight = {
			OR = {
				owns_or_vassal_of = 410 # Bagdad
				owns_or_vassal_of = 385 # Mekkah
				owns_or_vassal_of = 384 # Medinah
				owns_or_vassal_of = 382 # Dimashq
				owns_or_vassal_of = 361 # Cairo
				owns_or_vassal_of = 379 # Al-Quds				
			}
		}
		allow = {
			religion_group = muslim
		#	owns_or_vassal_of = 504
		#	owns_or_vassal_of = 225
			owns_or_vassal_of = 379 # Al-Quds
			owns_or_vassal_of = 361 # Cairo
			owns_or_vassal_of = 410 # Bagdad
			owns_or_vassal_of = 385 # Mekkah
			owns_or_vassal_of = 384 # Medinah
			owns_or_vassal_of = 382 # Dimashq
		#	owns_or_vassal_of = 124
		#	owns_or_vassal_of = 125
		#	owns_or_vassal_of = 425
		#	owns_or_vassal_of = 454
		#	owns_or_vassal_of = 347
		#	owns_or_vassal_of = 388
		#	owns_or_vassal_of = 400
		}
		effect = {
			if = {
				limit = {
					NOT = {
						government_rank = 3
					}
				}
				set_government_rank = 3
			}
			override_country_name = THE_CALIPHATE
			set_country_flag = unified_islam
			set_global_flag = unified_islam_global
			if = {
				limit = {
					government = monarchy
					NOT = {
						has_reform = feudal_theocracy
					}
					is_revolutionary = no
				}
				add_government_reform = feudal_theocracy
			}

			mashriq_region = {
				limit = {
					NOT = { owned_by = ROOT }
					NOT = { is_core = ROOT }
				}
				add_permanent_claim = ROOT
			}
			egypt_region = {
				limit = {
					NOT = { owned_by = ROOT }
					NOT = { is_core = ROOT }
				}
				add_permanent_claim = ROOT
			}
			arabia_region = {
				limit = {
					NOT = { owned_by = ROOT }
					NOT = { is_core = ROOT }
				}
				add_permanent_claim = ROOT
			}
			add_country_modifier = {
				name = unified_islam
				duration = -1
			}
		}
		ai_will_do = {
			factor = 1
		}
	}
	
	change_religion_sunni = {
		potential = {
			has_dlc = "Star and Crescent"
			NOT = { religion = sunni }
			dominant_religion = sunni
			
			is_force_converted = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			dominant_religion = sunni
			stability = 1
			is_at_war = no
		}
		effect = {
			add_stability = -1
			add_prestige = -50
			add_piety = -1
			change_religion = sunni
		}
		ai_will_do = {
			factor = 0
		}
	}
	
	change_religion_shiite = {
		potential = {
			has_dlc = "Star and Crescent"
			religion = sunni
			dominant_religion = shiite
			is_force_converted = no
		}
		allow = {
			is_free_or_tributary_trigger = yes
			dominant_religion = shiite
			stability = 1
			is_at_war = no
		}
		effect = {
			add_stability = -1
			add_prestige = -50
			add_piety = -1
			change_religion = shiite		
		}
		ai_will_do = {
			factor = 1
		}
	}
}